# Core dependencies for the Deep Ensembles reproduction.
# Tested with Python 3.10+.
torch>=2.0
numpy>=1.23
matplotlib>=3.6
pandas>=1.5
scikit-learn>=1.2
tqdm>=4.64

# For running the notebooks.
jupyter>=1.0
ipykernel>=6.0

# Required only by the UCI dataset loader (reads .xls / .xlsx files).
openpyxl>=3.1
xlrd>=2.0
